import generateImage from './generate-image.js';

const width = 10;
const height = 10;
const targetSizeMb = 10;
const format = 'png';
const outputPath = null;

void generateImage(width, height, targetSizeMb, format, outputPath);
